# ComicVeil — アプリ仕様書

最終更新日：2026年9月24日

---

## 目次

1. [プロジェクト概要](#1-プロジェクト概要)
2. [設計思想](#2-設計思想)
3. [技術スタック](#3-技術スタック)
4. [画面構成](#4-画面構成)
5. [機能仕様](#5-機能仕様)
6. [UI・UX仕様](#6-uiux仕様)
7. [データ設計](#7-データ設計)
8. [対応フォーマット](#8-対応フォーマット)
9. [開発フェーズ](#9-開発フェーズ)
10. [未決事項](#10-未決事項)

---

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| アプリ名 | **ComicVeil** |
| パッケージ名 | com.kamneko88.comicveil |
| ジャンル | マンガ・コミックビューワー |
| 対象プラットフォーム | Android（メイン）、将来的にWindows |
| 開発言語 | Kotlin |
| UIフレームワーク | Jetpack Compose + Material Design 3 |
| 開発環境 | Android Studio |
| プロジェクト保存先 | D:\Data\10_Projects\dev\ComicVeil |
| GitHubリポジトリ | kamneko88/ComicVeil（**Public**） |
| 配信先 | **Google Play Store（製品版を配信中・2026年9月開始）** |
| 最小SDKバージョン | API 26（Android 8.0） |
| 現在バージョン | **v1.17.0（開発版・2026年9月24日時点。公開中バージョンとは異なる）** |

### コンセプト

> 「Androidで一番まともなマンガビューワー」
>
> iOS用Comic Glassへの最大限のリスペクトを明示しつつ、
> Androidにおける同等の快適な読書体験の実現を目指す。

---

## 2. 設計思想

### 5つの設計原則

| 原則 | 内容 |
|---|---|
| **① 安定性最優先** | クラッシュしない。NAS接続断もクラッシュではなく「状態」として扱う |
| **② 触らなくても快適** | デフォルト設定で十分快適に読める |
| **③ 触ればもっと自分好みに** | 設定を深く潜れば細かくカスタマイズできる |
| **④ よく使う設定はプリセットで一発** | 複数のプリセットを保存・即時切り替え |
| **⑤ UIは自分好みにカスタマイズできる** | ツールバーの中身・表示タイミングを変更可能 |

### NASファーストの思想

```
NASもローカルも「場所」として統一的に扱う
ファイルは参照するだけ・汚さない・移動させない
メタデータ（評価・タグ等）はアプリ内DBで管理
サムネイルのみキャッシュとして内部に保存
```

### Comic Glassリスペクト

本アプリはiOS用Comic Glassに強くインスパイアされている。
UIの設計思想・操作概念を参考にしており、アプリ説明欄にその旨を明記する。

---

## 3. 技術スタック

### コア

| 用途 | ライブラリ | 理由 |
|---|---|---|
| 画像表示 | **Coil 3** | Kotlin製・メモリ管理優秀・キャッシュ機能◎ |
| ZIP/CBZ展開 | **Apache Commons Compress** | Shift-JIS対応・Progressive Loading対応 |
| RAR/CBR展開 | **junrar（RAR4）／libarchive-android（RAR5）** | ファイルの中身からRARのバージョンを判定して振り分ける。junrarはRAR5非対応、libarchiveはAndroidで日本語ファイル名の取得が不安定なため（詳細は開発ノート） |
| 7z展開 | **Apache Commons Compress ＋ tukaani xz** | 実装済み（v0.18.0）。LZMA/XZ の展開に xz の明示追加が必要 |
| PDF表示 | **Android標準 PdfRenderer** | 実装済み（v0.18.0） |
| パスワード付きZIP・7z | **zip4j／Apache Commons Compress** | 検知・復号とも対応済み（2026-09-16）。パスワード付きRAR・PDFは非対応（詳細は§5・§8） |
| SMB接続（NAS） | **SMBJ 0.13.0** | Android実績あり |
| 共有フォルダ一覧取得 | **rapid7 dcerpc 0.12.13** | srvsvc/MSRPCでNASの共有名を列挙 |
| データベース | **Room 2.7.0（SQLite）** | Android公式ORM |
| SAFフォルダ操作 | **androidx.documentfile 1.0.1** | Storage Access Framework経由のフォルダ選択・操作 |
| 生体認証 | **androidx.biometric 1.1.0** | アプリロックの指紋・顔認証（PINは端末内ソルト付きハッシュで別途検証） |
| フォアグラウンド/バックグラウンド検知 | **androidx.lifecycle-process 2.10.0** | アプリ全体単位でのアプリロック再ロック判定に使用 |
| ナビゲーション | **androidx.navigation-compose 2.7.7** | 画面遷移（Home・閲覧・設定・バックアップ・履歴など） |
| アニメーション | **Compose spring()** | HorizontalPagerのflingBehaviorで実装 |

### 開発補助

| 用途 | ツール |
|---|---|
| ローカルLLM | Ollama + Qwen3:14b |
| GUIチャット | Open WebUI |
| エディタ内補助 | Continue（Android Studioプラグイン） |
| バージョン管理 | Git + GitHub |

---

## 4. 画面構成

### 画面一覧

```
① ホーム画面（Home）
② フォルダ・ファイルブラウザ画面（Home以外）
③ 閲覧画面（通常）
④ 閲覧画面（タップ後・メニュー表示）
⑤ 設定画面
⑥ リモートサーバー設定画面
⑦ 複数巻アーカイブの巻一覧画面
⑧ 転送状況画面
⑨ 閲覧履歴画面
⑩ バックアップと復元画面
⑪ アプリロック画面（ロック解除・PIN設定）
```

※ プリセット選択・切り替え機能は未実装（詳細は§5「プリセット機能」）。

---

### ① ホーム画面

#### 上部メニュー
```
[HOME]　　　　　　　　　[🔍] [名前▼] [表示切替] [編集]
　　　　　　　　　　　　検索  ソート  詳細/簡易/本棚  編集モード
```

#### ファイル・フォルダ表示エリア
- ローカル（アプリ専用フォルダ／SAFで選択したフォルダ）のコミックファイル
- 登録済みNASサーバー一覧
- ショートカット登録したリモートフォルダ・本（HOMEへのブックマーク登録。長押しメニューから登録/解除）
- お気に入り登録したコミックファイル（未実装）

#### 下部メニュー（通常時）
```
[⋮:その他] [リモート追加] [STR/DL切替（NAS内のみ）] [転送状況] [設定]
```

| ボタン | 内容 |
|---|---|
| **⋮：その他** | 閲覧履歴／バックアップと復元／このアプリについて、をドロップダウンで表示 |
| **リモート追加** | NASサーバーの新規登録ダイアログを開く |
| **STR/DL切替** | NASのフォルダを開いているときのみ表示。ストリーミングモード／ダウンロードモードの切り替え |
| **転送状況** | ダウンロード・取り込みの転送状況画面へ |
| **設定** | 設定画面へ |

明るさ調整はビューワー画面内にのみ存在し、HOME画面の下部メニューにはない。

#### ソート・フィルター（BottomSheet）
- 「名前▼」ボタンタップでBottomSheetを表示
- **ソート**：名前 / 更新日 / レーティング（各々▲▼で昇降順）
- **フォルダ表示順**：フォルダ優先 / ファイル優先 / 混在
- **フィルター**：読書状態 / カラーラベル（ローカル・NAS共通）

#### 表示切替ボタン
タップのたびに **詳細表示 → コンパクト表示 → 本棚表示 → 詳細表示…** の順に巡回する。
- **詳細表示**：サムネイル大・タイトル+作者名・各種バッジ・更新日
- **コンパクト表示**：サムネイル小・ファイル名1行・読書状態バッジのみ
- **本棚表示**：表紙のみを棚板状に並べる。設定でタイトル表示のON/OFFを切替可能

---

### ② フォルダ・ファイルブラウザ画面（Home以外）

#### 上部メニュー
```
[←] [現在のフォルダ位置]　　　[🔍] [名前▼] [≡⊞] [編集]
　長押しでサーバールートまたはHomeに戻るボタン表示
```

#### 下部メニュー

ホーム画面と同じ構成（[⋮:その他] [リモート追加] [STR/DL切替（NASのフォルダのみ）] [転送状況] [設定]）。
編集モード中のみ、ローカルフォルダに限り以下に切り替わる。

**ローカルフォルダの編集モード時：**
```
[削除]　　　　　　　　　　N件選択中 / ファイルを選択してください
```
上部メニューの「全選択／選択解除」「完了」と組み合わせて使う。移動・フォルダ作成機能は無い。

**リモートフォルダ時：**
編集モードによる複数選択・削除はない（安全のため）。ファイルを長押しすると
「HOMEに登録／解除」「情報を見る」のコンテキストメニューが出る。まとめてダウンロードしたい場合は
DLモードでファイルをタップして選択する（次項）。

#### DLモードのファイル選択
- ファイルをタップ → 選択状態（背景色変更）、複数選択可
- フォルダはタップで通常通り開く（選択対象外）
- 1件以上選択時 → 下部メニュー上に「選択ファイルをダウンロード（N件）」ボタンが出現
- ダウンロード後・DLモード解除時に選択リセット

---

### ③ 閲覧画面（通常）

```
全画面表示でコミックのページのみを表示
UIは完全に非表示
```

---

### ④ 閲覧画面（タップ後・メニュー表示）

#### タップ後の演出
- ページ表示がやや暗くなる（ふわっとアニメーション）
- 中央に「本を閉じる」ボタンが出現
- 下部メニューがスライドして出現

#### ズーム操作
- **ダブルタップ**：等倍 ↔ 設定倍率（120% / 135% / 150%）のトグル
- **ピンチイン/アウト**：任意倍率（最大3倍）
- **ズーム中パン**：指で表示位置を移動
- **ページ移動後**：ズームリセット（将来的に設定で制御予定）
- **ズーム中**：ページ送りスワイプ無効（パン操作優先）

#### ページの長押し
- 表示中のページを長押しすると「表紙に設定」の確認ダイアログが出る
- 確定すると、そのファイルの一覧表示用サムネイルを現在のページに差し替える
- 複数巻アーカイブの巻ビュー（⑦）では対象外

#### 下部メニュー（閲覧時専用）
```
左グループ：[ブックマーク一覧] [このページをブックマーク] [ページ移動（サムネイルストリップ）]
右グループ：[明るさ] [画面の向きをロック] [設定]
```

| ボタン | 内容 |
|---|---|
| **ブックマーク一覧** | 登録済みブックマークをサムネイル付きで表示・削除・全削除 |
| **このページをブックマーク** | 現在ページをブックマーク登録（登録済みは色変化） |
| **ページ移動** | サムネイルストリップを展開してページジャンプ |
| **明るさ** | 明るさスライダーを展開（アプリ全体で記憶） |
| **画面の向きをロック** | 現在の向きに端末の回転を固定 |
| **設定** | 設定画面へ |

---

### ファイル・フォルダの表示仕様

#### 詳細表示（デフォルト）

```
┌──────────────────────────────────────┐
│ [表紙  ] タイトル名                   │
│ サムネイル 作者名                     │
│         ファイル形式                  │
│         更新日（今日/昨日/〇日前/日付）│
│ [読書状態] [Cached] [Ⓘ]              │
└──────────────────────────────────────┘
```

#### 簡易表示

```
[小サムネ] ファイル名（1行・末尾省略）  [状態] [Ⓘ]
```

#### ファイル情報ポップアップ（Ⓘタップ時）
- ファイル名（タイトル）・作者名・ファイルサイズ・更新日・形式
- 状態：未読／読書中／既読（タップで切り替え・「読書中」は手動選択不可）
- レーティング（★1〜5、「なし」で解除）
- カラーラベル（赤／橙／緑／青／紫）

#### ファイル名からのタイトル・作者名抽出

```
対応パターン：
(カテゴリ) [作者名] タイトル.zip

例：(一般コミック) [桂正和] ウイングマン1巻.zip
→ 作者名：桂正和
→ タイトル：ウイングマン1巻
→ (カテゴリ)部分は非表示

パターン不一致の場合：ファイル名をそのまま表示
```

---

### STRモードのタップポップアップ

```
未キャッシュ時：
・最初から読む

キャッシュ済み（読書位置なし）：
・最初から読む

キャッシュ済み（読書位置あり）：
・○○ページから再開
・最初から読む
```

---

### 編集モード

#### 発動条件
- 上部メニューの「編集」ボタンをタップ

#### 編集モード

**ローカルフォルダ時：**
上部メニューに「全選択／選択解除」「完了」、下部メニューに「削除」ボタンが出る。
ファイル未選択時は削除がグレーアウト。移動・フォルダ作成機能は無い（未実装）。

**リモートフォルダ時：**
編集モードによる複数選択・削除は無い（リモートファイルの安全性確保）。
まとめてダウンロードしたい場合はDLモードでファイルをタップして選択する。

---

## 5. 機能仕様

### ストレージ・ファイルアクセス

#### 「場所」の種類

| 種類 | 例 | 特徴 |
|---|---|---|
| NAS（SMB接続） | //192.168.1.10/manga/ | ストリーミング or DL |
| ローカルフォルダ | /sdcard/Comics/ | 直接参照 |
| アプリ専用フォルダ | `Android/data/com.kamneko88.comicveil/files/Comics` | 転送ファイルの保管場所（Homeフォルダの既定値でもある） |

#### 内部ストレージの使い方

```
保存するもの：
・SQLiteデータベース（評価・カラーラベル・読書位置・ブックマーク・設定）
・サムネイルキャッシュ（ローカル・NAS両対応）
・NASキャッシュ（STRモード時の一時ファイル / ページ単位キャッシュ）
・アプリ設定（SharedPreferences）

保存しないもの：
・コミックファイル本体（参照のみ・DLモード時を除く）
```

#### DL/STRモード

| モード | 保存先 | 特徴 |
|---|---|---|
| STR（ストリーミング・デフォルト） | アプリキャッシュ（一時） | ZIP：1ページ目から即閲覧開始（Progressive Loading）、RAR：全DL後 |
| DL（ダウンロード） | アプリ専用フォルダ（`Android/data/com.kamneko88.comicveil/files/Comics`・永続） | ファイルブラウザからも参照可能。保存先は固定で設定からの変更は不可（§10参照） |

#### ZIP Progressive Loading（STRモード）
- ZipArchiveInputStreamでNASからストリームを読み込みながらページを逐次書き出し
- 1ページ目が届いた時点でビューワーを起動
- ページはファイルパス（String）として管理しメモリを節約
- RARは従来通り全DL後に表示（将来的にProgressive対応予定）

#### Intent対応（他アプリからの受け取り）
- ファイルマネージャー等から「ComicVeilで開く」でZIP/RARを直接起動
- content URI → キャッシュにコピー、file URI → そのまま参照

---

### 読書状態管理

#### 状態の定義

| 状態 | 条件 |
|---|---|
| **未読** | 一度も開いていない、または先頭ページから送っていない |
| **読書中** | 1ページ以上送り、最終ページ未到達 |
| **既読** | 最終ページに到達している |

#### 自動遷移ルール

| 操作 | 結果 |
|---|---|
| コミックを開く（先頭ページ） | 状態変化なし（未読のまま） |
| 1ページ以上送る | 読書中 |
| 最終ページに到達 | 既読 |
| 最終ページから1枚以上戻す | 読書中 |

#### 手動変更時のページ位置連動

| 変更 | ページ位置の扱い |
|---|---|
| → 未読 | 1ページ目にリセット |
| → 既読 | 最終ページに設定 |
| → 読書中 | 手動選択不可（自動遷移のみ） |

---

### サムネイル管理

| 操作 | 対象 | アクセス方法 |
|---|---|---|
| 全体再作成（間接的） | ライブラリ全体 | Settings → キャッシュ管理でサムネイルキャッシュを削除（次回表示時に再生成） |
| 任意ページを表紙に設定 | 1ファイル | ビューワーでページを長押し |

#### NASサムネイルの取得順序
1. サムネイルキャッシュ（thumbnails/nas_*.jpg）があれば即返却
2. STRキャッシュ（ページキャッシュ）があればそこから生成
3. NASから先頭2MBのみ取得してZIPの先頭画像を抽出
4. RAR・パスワード付きZIPはスキップ（アイコン表示）

---

### ファイル転送

| 方法 | 状態 | 説明 |
|---|---|---|
| USB転送（PC→スマホ） | ✅ 対応 | アプリ側対応不要 |
| 他アプリからの共有（Intent） | ✅ 実装済み | 「ComicVeilで開く」で受け取り |
| NASから直接ストリーミング | ✅ 実装済み | Progressive Loading対応 |
| NASからダウンロード | ✅ 実装済み | ファイル選択式・複数選択可 |
| 外部ファイルの取り込み | ✅ 実装済み | クラウドサービス等でダウンロードしたファイルを専用UIから取り込み |
| Wi-Fi経由転送UI | ⬜ Phase 2 | アプリ内Webサーバー機能 |

---

### ソート・フィルター機能

#### ソートキー
- 名前（昇順/降順）
- 更新日（新しい順/古い順）
- レーティング（高い順/低い順）

#### フォルダ表示順
- フォルダ優先 / ファイル優先 / 混在

#### フィルター（ローカル・NAS共通）
- 読書状態（未読/読書中/既読）
- カラーラベル（色指定）

---

### タグ・評価機能（Lightroomライク）

#### 評価（実装済み）
- ★1〜5の5段階

#### カラーラベル（実装済み）
- 赤／橙／緑／青／紫
- デフォルト名あり（変更可能：未実装）

#### フラグ（未実装）
- ピック（✓）：絶対残す
- リジェクト（✗）：削除候補

#### タグ（キーワード）（未実装）
- 自由入力・複数付与可能

---

### プリセット機能（未実装）

複数の設定を1つにまとめてワンタップで切り替える「プリセット」自体はまだ実装していない。
ただし、当初プリセットの核として想定していた項目の多くは、個別の設定として先行実装済み
（§6設定画面参照）：ページ送り方向・ダブルタップズーム率・見開き表示・見開き分割・綴じ代・
背景色・余白削除（トリミング）・ページ送りアニメーション・音量ボタンでのページ送りなど。

#### プリセット数
- A〜E（仮）の5つ（未確定）

---

### 読書位置の自動保存

- 閲覧中は常にページ位置をDBに保存
- アプリがクラッシュしても再起動時に再開可能
- STRキャッシュ済みファイルは「○○ページから再開」ポップアップで表示

---

### バックアップ／復元機能（実装済み）

その他メニュー →「バックアップと復元」からアクセスする。

#### 対象5項目（`BackupItemType`）
| 項目 | jsonKey | 内容 |
|---|---|---|
| 設定 | settings | 読む・見た目・キャッシュ上限などの設定（Homeフォルダ／DLフォルダ／アプリロックは含まない） |
| 読書記録 | readingHistory | 各本の読書位置・総ページ数・最終閲覧日時・元のファイル名 |
| 評価・カラーラベル・既読状態 | ratingsLabels | ★評価・カラーラベル・未読/読書中/既読 |
| しおり | bookmarks | 本の中に挟んだしおり |
| NASサーバー設定 | nasServers | 登録NASサーバーとHOMEショートカット。パスワードを含める場合はパスフレーズで暗号化 |

作成時にこの5項目から選んでバックアップでき、復元時も同様に項目を選べる。

#### 保存形式
- 1つのJSONファイル（`schemaVersion`付き。現在の値は1）。選択しなかった項目のキーはnullで省略される
- 保存先はSAF（`ActivityResultContracts.CreateDocument` / `OpenDocument`）経由でユーザーが選ぶ
- ファイルサイズの上限は50MB

#### 暗号化
- NASサーバーのパスワードを含める場合のみ、パスフレーズ（8文字以上）から
  PBKDF2WithHmacSHA256（反復310,000回）で鍵を導出し、AES-256-GCMで暗号化する
- パスフレーズが誤っている場合は復元時にエラーとして検知される（AEADタグ不一致）

#### 復元の方式（マージ・非破壊）
- 読書記録・評価/カラーラベル・既読状態：既存データは残したまま、同じ本の記録はバックアップの内容で上書き（upsert）
- しおり：ファイルパス＋ページの組が既存に無いものだけを新規追加（重複は追加しない）
- NASサーバー：IDが一致すれば情報を更新（パスワードは復元で明示的に渡されない限り保持）、無ければ新規追加
- HOMEショートカット（リモートブックマーク）：サーバーID＋パスの組が既存に無いものだけを新規追加
- 既存のデータが削除されることはない

#### 復元に含まれないもの
- PINハッシュ・ロック設定・SAFで選択したフォルダのURI（端末固有のため）

#### アプリの再起動
- 設定またはNASサーバー設定（NAS本体・HOMEショートカットのいずれか）を復元した場合、
  画面側でキャッシュ済みの状態（ソート条件・NASサーバー一覧等）を持つ画面があるため、
  復元完了後にアプリが起動し直される

---

### アプリロック機能（実装済み）

設定画面「セキュリティ」からPIN／生体認証によるロックを設定できる。

- モード：OFF／PINのみ／生体認証のみ（失敗時はPINでも解除可）／PIN・生体認証の両方
- PINは平文で保存せず、SHA-256のソルト付きハッシュとして`AppPrefs`（SharedPreferences）に保存する
- 生体認証は`androidx.biometric`（指紋・顔）
- アプリ全体（個々のActivityではなく`ProcessLifecycleOwner`単位）でバックグラウンド遷移を検知し、
  一度でもバックグラウンドへ行くと再ロックする
- ロック解除・PIN設定はそれぞれ専用画面（ロック画面・PIN設定画面）で行う

---

### 配色テーマ（実装済み）

設定画面「外観」からダーク／白系を切り替えられる。
- 対象はHOME・設定・履歴・転送などアプリ全体のUI
- **閲覧画面（ビューワー本体）は対象外で、テーマ設定に関わらず常にダーク固定**

---

## 6. UI・UX仕様

### アニメーション

#### ページ送りアニメーション
- **デフォルト**：スライド＋スプリング（バウンス）
- Compose の `spring()` + `PagerDefaults.flingBehavior` で実装

#### メニューのアニメーション
- タップ時：画面が暗くなり（alpha 0→0.4）、下部メニューがスライドで出現
- AnimatedVisibility（Jetpack Compose）で実装

---

### 設定画面（実装済み項目）

#### 「読む」セクション
| 設定項目 | 内容 |
|---|---|
| ページ送り方向 | 右綴じ（マンガ）／左綴じ（洋書・縦読み） |
| ページ送りアニメーション | OFFで即座に切り替わる（非力な端末向け） |
| ページ送りのバウンス | スワイプ時にぶるんと戻る動きのON/OFF |
| 音量ボタンでページ送り | 音量UP＝前のページ、音量DOWN＝次のページ |
| ズームバウンス | ピンチズームの最大・最小倍率でバウンスするか |
| ダブルタップズーム率 | 120% / 135% / 150% |
| 見開き表示 | しない／横向きのときだけ／常に見開き（ONのとき「表紙を単独表示」「見開きの綴じ代（0〜10%）」を追加表示） |
| 見開き分割 | 見開き表示OFFのときのみ表示。横長ページを自動で単ページ2枚に分割 |
| 余白を削除 | しない／削除する／白い余白のみ（ONのとき「縦横比を維持」を追加表示） |
| 背景色 | 黒／白／グレー |
| 本棚にタイトルを表示 | 本棚表示モードで表紙の下にタイトルを出すか |

#### 「ファイル・フォルダ」セクション
| 設定項目 | 内容 |
|---|---|
| Homeフォルダ | ComicVeilフォルダ（アプリ専用領域・推奨）／SAFで選んだ任意のフォルダ |
| DLフォルダ | ComicVeilの外（クラウドサービス等）でダウンロードしたファイルを取り込むためのSAFフォルダ |

NASからのダウンロード保存先は常にアプリ専用フォルダ固定で、設定からは変更できない。

#### 「キャッシュ管理」セクション
| 設定項目 | 内容 |
|---|---|
| NASストリーミングキャッシュ | サイズ表示・削除、上限（500MB〜5GB／無制限） |
| サムネイルキャッシュ | サイズ・枚数・内訳（NAS／端末内／外部フォルダ）表示、削除 |
| ページキャッシュ | サイズ表示・削除、上限（500MB〜5GB／無制限） |

#### 「外観」セクション
| 設定項目 | 内容 |
|---|---|
| 配色テーマ | ダーク／白系（ビューワー画面は対象外で常にダーク） |

#### 「セキュリティ」セクション
| 設定項目 | 内容 |
|---|---|
| アプリロック | ON/OFF。ONで未設定ならPIN設定画面へ誘導 |
| ロック方式 | PINのみ／生体認証のみ／PIN・生体認証の両方（ONのときのみ表示） |
| PINを設定／変更 | PIN設定画面を開く |

#### バージョン情報
| 設定項目 | 内容 |
|---|---|
| バージョン表示 | 設定画面最下部に `ComicVeil vX.Y.Z` の形式で表示 |

---

### ツールバーカスタマイズ（未実装）

**表示タイミング：**
- タップで出現（デフォルト）
- 常時表示
- 端をスワイプで出現

---

## 7. データ設計

### DBテーブル構成（v5現在）

```sql
-- 読書位置（実装済み）
reading_progress
  filePath（PK） / currentPage / totalPages / lastReadAt

-- ファイルのメタデータ（v4・実装済み）
files
  filePath（PK） / status（未読0/読書中1/既読2） / rating（0〜5）
  colorLabel（0=なし/1=赤/2=橙/3=緑/4=青/5=紫） / registeredAt

-- ブックマーク（v3・実装済み）
bookmarks
  id / filePath / page / createdAt

-- リモートの本の元の名前（v5・実装済み）
file_titles
  filePath（PK） / originalName

-- タグ（未実装）
tags
  id / 名前

-- ファイルとタグの紐付け（未実装）
file_tags
  file_id / tag_id

-- プリセット（未実装）
presets
  id / 名前（A〜E）/ 設定JSON
```

### DB マイグレーション履歴
```
v1 → v2：files テーブル追加
v2 → v3：bookmarks テーブル追加
v3 → v4：files に rating / colorLabel カラム追加
v4 → v5：file_titles テーブル追加（リモートの本の元の名前を覚える）
```

### SharedPreferences 設定項目（AppPrefs）
用途ごとの分類のみ示す（PINハッシュ・ソルトの中身は非公開情報のため記載しない）。
```
【ホーム／DLフォルダ】
homeFolderType / homeFolderSafUri / downloadFolderSafUri

【ページ送り・ズーム】
pageDirection（RIGHT_TO_LEFT / LEFT_TO_RIGHT）
pageAnimation・pageTurnAnimation・volumeKeyPageTurn・zoomBounce
doubleTapZoom（ZOOM_120 / ZOOM_135 / ZOOM_150）

【見開き・余白・背景】
spreadMode（OFF / LANDSCAPE / ALWAYS）・spreadCoverSingle・spreadGutter・splitWidePages
trimMode（OFF / ON / WHITE_ONLY）・trimKeepAspect・backgroundColor（BLACK / WHITE / GRAY）

【表示モード】
listDisplayMode（DETAIL / COMPACT / SHELF）・shelfShowTitle

【明るさ・キャッシュ上限】
viewerBrightness・pageCacheLimit・nasStreamCacheLimit

【外観・アプリロック】
appTheme（DARK / LIGHT）
lockMode（OFF / PIN_ONLY / BIOMETRIC_ONLY / BOTH）
PINのハッシュ・ソルト（値の詳細は非公開）
```

### ソート設定（SortPrefs）
```
sortKey      ：NAME / DATE / RATING
ascending    ：Boolean
folderOrder  ：FOLDER_FIRST / FILE_FIRST / MIXED
statusFilter ：Set<String>
colorLabelFilter：Set<String>
```

### NASサーバー設定・リモートブックマーク
- Room ではなく SharedPreferences + JSON で管理（NASサーバー一覧・HOMEに登録したリモートのフォルダ／本のショートカットとも）

---

## 8. 対応フォーマット

### 画像フォーマット

| カテゴリ | フォーマット | 優先度 |
|---|---|---|
| Web主流 | JPEG, PNG, WebP, GIF | 最優先 |
| 次世代 | HEIC, AVIF, JPEG XL | 高 |
| AI生成 | PNG（tEXtチャンク）, WebP | Phase 2 |

### アーカイブフォーマット

| 形式 | 拡張子 | ライブラリ | 状態 |
|---|---|---|---|
| ZIP | .zip / .cbz | Apache Commons Compress | ✅ 実装済み（Progressive Loading対応）。**.cbz は実機で動作確認済み** |
| RAR | .rar / .cbr | junrar（RAR4）／libarchive-android（RAR5） | ✅ 実装済み。RAR5＋日本語ファイル名は、libarchiveでの名前取得が失敗する既知の制約があるため、読み取り順の位置から合成した名前で代替する |
| 7-Zip | .7z | Apache Commons Compress ＋ tukaani xz | ✅ 実装済み（v0.18.0） |
| PDF | .pdf | Android標準 PdfRenderer | ✅ 実装済み（v0.18.0） |
| **パスワード付きZIP・7z** | .zip / .cbz / .7z | zip4j／Apache Commons Compress（検知）＋各ライブラリのパスワード指定（復号） | ✅ **実装済み（2026-09-16）**。開くとパスワード入力ダイアログを表示 |
| **パスワード付きRAR** | .rar / .cbr | junrar／libarchive（検知のみ） | ❌ **対象外**（RAR4形式のテストファイルを用意できず実機検証ができないため、2026-09-16時点で非対応と判断。専用メッセージを表示） |
| **パスワード付きPDF** | .pdf | Android標準 PdfRenderer | ❌ **対象外**（復号を行うAPIが無いため非対応。専用メッセージを表示） |

---

## 9. 開発フェーズ

### Phase 1：マンガビューワーとして完成させる

```
✅ プロジェクト作成・GitHub連携
✅ 基本的なファイルブラウザ（ローカル・NAS）
✅ ZIP/CBZ・RAR/CBR対応
✅ ZIP Progressive Loading（STRモード・1ページ目から即表示）
✅ 7z・PDF対応（v0.18.0）
✅ パスワード付きZIP・7zの検知・復号に対応（2026-09-16）
❌ パスワード付きRAR・PDF対応 → **対象外**（検証環境の制約・API制約のため非対応）
✅ 快適なページ送り（スプリングアニメーション）
✅ ズーム機能（ダブルタップ・ピンチ・パン）
✅ 読書位置の自動保存
✅ SMB接続（NASアクセス）
✅ NASサムネイル自動生成
✅ DL/STRモード切り替え（DLモード：ファイル選択式）
✅ 上下メニューのスライドアニメーション
✅ サムネイル表示（ローカル・NAS対応）
✅ 読書状態管理（未読/読書中/既読）・自動遷移・バッジ表示
✅ ファイル情報ポップアップ（Ⓘボタン）
✅ レーティング・カラーラベル機能
✅ ソート機能（名前・更新日・レーティング・フォルダ優先）
✅ フィルター機能（読書状態・カラーラベル・ローカルNAS共通）
✅ 詳細表示・簡易表示切り替え
✅ ブックマーク機能
✅ 明るさ調整
✅ STR再開ダイアログ（未キャッシュ/キャッシュ済みで表示切替）
✅ 他アプリからのファイル受け取り（Intent）
✅ 設定画面（基本設定・キャッシュ管理・ズーム率）
✅ 検索機能（現在フォルダ・HOME内）
✅ 本棚表示モード（3つ目の一覧表示モード）
✅ 複数巻アーカイブ対応（巻フォルダを検出し巻一覧画面から選択）
✅ ファイルの中身（シグネチャ）を見て正しい形式で開く
✅ 外部ファイルの取り込み機能（クラウドピッカーUI）
✅ バックアップ／復元機能（項目別選択・NAS情報はパスフレーズで暗号化）
✅ アプリロック（PIN・生体認証）
✅ 配色テーマ（ダーク／白系。ビューワーは常にダーク固定）
✅ NASストリーミングキャッシュ・ページキャッシュの上限とLRU削除
⬜ プリセット機能（個々の設定項目は先行実装済み。§5参照）
⬜ 設定画面の完成（物理キー操作は実装済み。残りは調整中）
⬜ タグ機能
✅ SAF権限（MANAGE_EXTERNAL_STORAGEからの移行。v0.19.0で完了）
✅ Google Play クローズドテスト準備 → **2026年8月19日に開始**、2026年9月5日に製品版配信へ移行
⬜ Phase 1 最終調整・実機テスト
```

### Phase 2：機能追加

```
⬜ 読書ログ・進捗管理・統計（有料アドオン候補）
⬜ 画像ビューアーモード（単体画像表示・AI生成画像メタデータ表示）
⬜ ページめくりアニメーション（3D風）
⬜ Wi-Fi経由ファイル転送UI（アプリ内Webサーバー機能。外部ファイルの取り込みUI自体はPhase 1で実装済み）
⬜ スライドショー機能
⬜ シリーズ自動グルーピング
⬜ RARのProgressive Loading
```

### Phase 3：将来展開

```
✅ Play Store公開（2026年9月5日に製品版として正式リリース。配信範囲は全世界177か国／地域）
⬜ クラウドストレージ連携（有料アドオン候補）
⬜ Windows版（技術スタック未定・Android版リリース後に検討）
※ iOS・Mac版はリリース対象外（ストア登録料の問題）
```

### 収益モデル

| 項目 | 内容 |
|---|---|
| 基本方針 | 無料アプリとしてPlay Store公開 |
| アドオン候補 | 統計・読書ログ ＋ クラウドストレージ連携（抱き合わせ） |
| 想定価格 | 200円前後 |
| 進め方 | Phase 1公開後、反応を見てからPhase 2/3と合わせてリリース |

---

## 10. 未決事項

- [x] アプリアイコンデザイン → **アダプティブアイコンで実装済み**（v0.26.0）
- [x] カラーテーマ → **ダーク／白系を切り替え可能**に決定・実装済み（ビューワー画面のみ常にダーク固定）
- [ ] プリセット数の上限（A〜E＝5つで仮決定）
- [x] 収益モデル → **無料＋アドオン（統計＋クラウド連携）**に決定
- [ ] 対応言語（日本語先行／英語同時対応）
- [x] DLモード時のファイル保存先 → **2択に変更**（2026-08 現在）。①アプリ専用領域（`Android/data/com.kamneko88.comicveil/files/Comics`・既定・推奨） ②ユーザーがSAFで選んだ任意のフォルダ。**当初の「Downloads/ComicVeil/」はSAF移行（v0.19.0）に伴い廃止**
- [ ] WebDAV対応（SMBのみで当面進める）
- [ ] タブレット横向きレイアウトの詳細
- [x] iOS・Mac版 → **リリース対象外**に決定
- [x] Windows版 → Android版リリース後に技術選定予定（先送り）
- [x] 画像ビューアーモード → **モード区別で将来実装**（Phase 2）に決定
- [x] テキスト・文庫ビューアー → **対象外**（別アプリの領域）に決定
- [ ] カラーラベル名のカスタマイズ
- [ ] 見開き表示の左右ページ重ね幅設定
- [ ] NASサーバー登録時の接続チェック / ブラウザ選択機能

---

*このドキュメントは開発進行に伴い随時更新される。*

最終更新日：2026年9月24日
